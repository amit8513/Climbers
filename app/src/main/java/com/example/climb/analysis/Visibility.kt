package com.example.climb.analysis

/** Who can see this climb attempt. Only PRIVATE is meaningfully enforced until sharing exists. */
enum class Visibility { PRIVATE, FRIENDS_ONLY, SELECTED_FRIENDS, PUBLIC }
